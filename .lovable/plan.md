## Diagnosis\n\n`fal-video` returns `Missing inputs (prompt + source + sourceKind required)` only when the body lacks `prompt` or `sourceUrl`/`sourceDataUrl`. The MCP `triggerJob` tool **already** accepts a `payload` object and forwards it verbatim into `action_jobs.payload`; the worker (`process-action-queue` lines 1047–1085) then maps `payload.prompt`, `payload.sourceUrl`, etc. onto the `fal-video` body.\n\nThe real problem is that the `payload` arg in the MCP tool schema is declared as a bare `{ type: \