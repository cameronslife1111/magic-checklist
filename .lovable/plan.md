## Goal\nWhen a checklist item with `media_url` is deleted (single or as part of a whole-checklist delete), the underlying file in the `generated-media` storage bucket must also be removed — UNLESS that file is a Media Gallery asset (tracked in `media_assets`), which the user wants to keep.\n\n## First-principles analysis\n\nThere are 3 sources of `media_url` values that end up on a `checklist_items` row:\n1. **AI-generated** by the worker / edge function — uploaded to `generated-media/${user_id}/<uuid>.png|mp4`. Not tracked in `media_assets`. **Should be deleted with the item.**\n2. **Media Gallery attachments** — uploaded to `generated-media/${user_id}/gallery/<uuid>.<ext>`. Tracked in `media_assets`. **Must NOT be deleted** (they live in the gallery and may be reused).\n3. **External URLs** (rare) — not in our bucket. The current `indexOf(\