# Auto-register generated media in the Gallery + use unique titles\n\n## Problem (first principles)\n1. The worker (`supabase/functions/process-action-queue/index.ts`) uploads generated images/videos into the `generated-media` bucket and inserts a checklist item — but it **never inserts a row into `media_assets`**. That's why generated outputs don't show up in the Media Gallery.\n2. The placeholder text above each generated output is a generic constant (`\