## Goal\nAdd a new **Split by emoji** action to the Actions sheet. It takes the highest unchecked checkbox's text and splits it into separate checkboxes at every point where an emoji appears (each emoji starts a new checkbox). After splitting, the new highest unchecked item is highlighted yellow and read aloud — matching the rest of the app's UX.\n\n## First-principles reasoning\nThe existing `splitCurrent()` flow in `src/pages/Checklist.tsx` already handles every concern we need: pick the source, compute fractional positions between the source and the next item, insert rows, delete the original, update local state, scroll, and speak. The **only** thing different for the new action is **how the text is split**. So the cleanest, lowest-risk approach is:\n\n1. Add a pure splitter `splitTextByEmoji` in `src/lib/split.ts`.\n2. Reuse the exact same insert/delete/scroll/speak machinery, generalized to take a splitter function.\n3. Wire a new `ActionKey` and a new sheet entry.\n\n## Splitter rules (`splitTextByEmoji`)\n- Detect emoji using the same Unicode property approach already used in `src/lib/speech.ts` (`\\p{Extended_Pictographic}`), combined with optional skin-tone modifiers, ZWJ joiners, regional indicators, and variation selectors so multi-codepoint emoji (👨‍👩‍👧, 👍🏽, 🇺🇸, ❤️) stay intact.\n- Walk the string and start a new chunk at every emoji boundary — the emoji belongs to the **new** chunk (per \