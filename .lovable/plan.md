# Strip down & rebuild the Run Sequence agent\n\n## Goal\n\nThe Run Sequence agent should do exactly **one thing per checklist line, in order**, with a tiny, clear set of inputs:\n\n1. The **single current line** it's working on.\n2. The **attached text context** (from the Run Sequence dialog).\n3. Its **toolbox** (the blue buttons: text, web-search, text→image, image→image, remix, image→video, video→video, audio+image→video, analyze-image).\n4. The **aspect ratio** (line-specified, otherwise the app default).\n5. The ability to **add a new checkbox** to the checklist with its output.\n\nNothing else. No compound/sub-steps, no nested children, no auto-discovered linked checklists, no name-fuzzy media matching, no \