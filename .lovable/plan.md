## Scroll highlighted checkbox to top instead of center\n\nWhen the highlighted (yellow) checkbox changes, scroll it so its **top edge aligns with the top of the viewport** instead of centering it. This way long checkboxes are readable from the first line.\n\n### Technical change\n\nSingle file: `src/pages/Checklist.tsx`, lines 254 and 262.\n\nChange both `scrollIntoView` calls from `block: \