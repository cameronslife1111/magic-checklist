# Fix `recurrence` schema in claude-mcp tools\n\n## What recurrence actually is (today)\n\nAfter reading `process-action-queue/index.ts` and `enqueue-action/index.ts`, the truth is simpler than the MCP currently implies:\n\n- `recurrence` is a **string enum**, not an object.\n- Allowed values: `\